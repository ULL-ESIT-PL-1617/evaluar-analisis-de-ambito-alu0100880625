module.exports = {
  PI: Math.PI,
  print: (...y) => console.log(...y.slice(0,-1)),
  abs: (x, y) => Math.abs(x),
  acos: (x, y) => Math.acos(x),
  acosh: (x, y) => Math.acosh(x),
  asin: (x, y) => Math.asin(x),
  asinh: (x, y) => Math.asinh(x),
  atan: (x, y) => Math.atan(x),
  atanh: (x, y) => Math.atanh(x),
  atan2: (x, y) => Math.atan2(x),
  cbrt: (x, y) => Math.cbrt(x),
  ceil: (x, y) => Math.ceil(x),
  clz32: (x, y) => Math.clz32(x),
  cos: (x, y) => Math.cos(x),
  cosh: (x, y) => Math.cosh(x),
  exp: (x, y) => Math.exp(x),
  expm1: (x, y) => Math.expm1(x),
  floor: (x, y) => Math.floor(x),
  fround: (x, y) => Math.fround(x),
  hypot: (x, y) => Math.hypot(x),
  imul: (x, y) => Math.imul(x),
  log: (x, y) => Math.log(x),
  log1p: (x, y) => Math.log1p(x),
  log10: (x, y) => Math.log10(x),
  log2: (x, y) => Math.log2(x),
  max: (x, y) => Math.max(x),
  min: (x, y) => Math.min(x),
  pow: (x, y) => Math.pow(x),
  random: (x, y) => Math.random(x),
  round: (x, y)  => Math.round(x),
  sign: (x, y) => Math.sign(x),
  sin: (x, y) => Math.sin(x),
  sinh: (x, y) => Math.sinh(x),
  sqrt: (x, y) => Math.sqrt(x),
  tan: (x, y) => Math.tan(x),
  tanh: (x, y) => Math.tanh(x),
  toSource: (x, y) => Math.toSource(x),
  trunc: (x, y) => Math.trunc(x)
};

